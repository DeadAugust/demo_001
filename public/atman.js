//useless for now, idk why

function Atman(x, y, fud){
  this.x = x;
  this.y = y;
  this.fud = fud;

  this.show = function(){
    fill(150,200,255);
    ellipse(this.x, this.y, 40, 40);
  }

  //this.inventory
  //this.trade
}
